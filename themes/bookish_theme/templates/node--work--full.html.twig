{% include 'sfc--mc-work' with {
  'title': node.label(),
  'subtitle': node.field_subtitle.value,
  'image': node.field_image|view({
    'label': 'hidden',
    'type': 'responsive_image',
    'settings': {'responsive_image_style': 'work_full'}
  }),
  'text': node.body|view({
    'label': 'hidden',
    'type': 'text_default',
  }),
} %}
{{ sfc_cache(node) }}
